@{
  Layout = "_Layout";
}

@using EauClairesSalon.Models;
@using System.Collections.Generic;
@model List<EauClairesSalon.Models.Client>;

<br>
<section class="section--center mdl-grid mdl-grid--no-spacing mdl-shadow--2dp">
  <div class="mdl-card mdl-cell mdl-cell--12-col">
    <div class="mdl-card__supporting-text mdl-grid mdl-grid">
      <h4 >All Clients</h4>
      @if (@Model.Count == 0)
      {
        <h3>No Clients have been added yet!</h3>
      } 
      <ul>
        @foreach(Client client in Model)
        {
          <hr>
          <li class="mdl-list__item mdl-list__item--three-line">
            <span class="mdl-list__item-primary-content">
              <i class="material-icons  mdl-list__item-avatar">person</i>
              <span>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId })</span>
              <span class="mdl-list__item-text-body">
                <p>Stylist: @client.Stylist.Name</p>
              </span>
            </span>
          </li>          
        }
      </ul>
    </div>
    <div class="mdl-card__actions">
      <button class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored">@Html.ActionLink("Home", "Index", "Home")</button>
    </div>
  </div>
</section>
<br>